/** Board empty state copy (UX_SPEC §2b — write it, don't improvise). */
export const BOARD_EMPTY_STATE = {
  message: 'The board fills when Phase 3 design is decomposed.',
  actionLabel: 'View current phase',
} as const;
