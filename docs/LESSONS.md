# Dokima — Field Lessons Ledger

One row per lesson; details below for the non-obvious ones. This file is the analyzable
input for (a) Dokima product features/tickets, (b) bpm-opencode-experts process/check
upstreams (two-way PRs, D-008), (c) bootstrap-harness fixes. Append-only; stable L-ids.
**Route-to** values: `product:<ticket/FR>` · `experts:<protocol/validator>` · `harness` ·
`process`. **Status**: `shipped` / `ticketed` / `open`.

| ID | Date | Class | Lesson (one line) | Route to | Status |
|---|---|---|---|---|---|
| L-01 | 07-11 | gate-economics | A single non-deterministic LLM review as gate authority fails both directions (75% false-block + 1 merged forgery in one session); deterministic validators own gates, LLM findings anchor review | product: D-014/FR-RL; experts: GATE_SCORING (already asymmetric) | shipped (D-014) |
| L-02 | 07-12 | loop-budgets | Stall ≠ progress ≠ oscillation — flat attempt caps punish progress and indulge stalls; budgets need finding identity + movement classes | product: FR-L6/L7, W3-08 (landed); experts: FIX_VERIFY v2.4.0 | shipped |
| L-03 | 07-12 | spec-writing | Acceptance criteria that NAME examples get the examples, not the set — recurred at W3-08 (4 of 6 policy rows) even after being written down | experts: add "ALL of the set" check to spec-review checklist + challenger prompt; product: challenger completeness pass standard at wave gates | open (check candidate) |
| L-04 | 07-12 | seams | The seam between two correct tickets is nobody's job (W1-02 invisible export; W0-08 undeclarable deps) | product: W5-07 seam-linter acceptance; W3-11 landed the backlog | ticketed |
| L-05 | 07-14 | traceability | ID-citation traceability green (81/81, 54/54) is NOT deliverable ownership — a fresh challenger found ~16 doc-mandated orphans invisible to the citation graph | process: challenger pass mandatory after every traceability green; product: coverage loop inventories deliverables, not just IDs | shipped (this arc) + open (product inventory rule) |
| L-06 | 07-14 | traceability | Cross-doc landing-wave claims drift silently (SC-06/07/08/09 all claimed waves that never happened) | experts: validator idea — grep "Lands: W<n>" claims vs plan wave reality; product: FR-RL provenance fields | open (validator candidate) |
| L-07 | 07-14 | board-schema | Done tickets holding write-scope territory forever makes every completed broad ticket a landmine (board failed its own FR-T3 validator 66×) | product: D-015 (territory releases at done + scaffold flag); experts: TICKET_SCHEMA/plan discipline upstream PR | shipped (D-015) / open (upstream) |
| L-08 | 07-14 | process | Founder-intent edits sitting uncommitted in the tree (tier-aware ceiling) are invisible decisions — commit-or-slate immediately | process; experts: session-start dirty-tree check (conductor preflight has it) | shipped (this arc) |
| L-09 | 07-14 | licensing | ToS/license research belongs at design review, not launch: Copilot `copilot_internal` flow risks USER account bans; semgrep registry rules are no-distribute; TruffleHog AGPL | product: D-019 consent gate; process: license research pass per integration wave | shipped (D-019) |
| L-10 | 07-14 | review-honesty | Never amend a done ticket's acceptance — it creates done-with-unmet-acceptance (caught by the challenger on my own W2-06 edit); new work gets a new ticket | process; experts: plan-edit rule in TICKET_SCHEMA; check candidate: flag acceptance edits on done tickets in CI diff | open (check candidate) |
| L-11 | 07-15 | harness-robustness | Schema drift inside one field (notes: string vs array) crashed markBlocked 15×, and each crash DESTROYED the block evidence — persist-evidence-before-status applies to harnesses too | harness (fixed); product: persist-before-execute already law (NFR-3); experts: BOUNDED_TASK_CONTRACT manifest typing | shipped (harness) |
| L-12 | 07-15 | seams | The seam class recurs even for the reviewer who threaded the rule: W3-12 as authored couldn't declare @dokima/shared deps (package.json outside scope) — the check must be mechanical, not remembered | product: W5-07 plan-linter (imports ⇒ package.json in scope); experts: validate-tickets seam heuristic; harness: none (scope widened) | ticketed (W5-07) |
| L-13 | 07-15 | verification | An agent hallucinated a doc path in ticket notes (docs/MODULE_DESIGN.md); the traceability validator caught it — but only because a human re-ran it | harness (fixed: both board validators now in the per-ticket gate); experts: cite-what-exists check on completion manifests | shipped (harness) |
| L-14 | 07-15 | tooling-discipline | `validator \| tail -1` swallows the exit code — a FAIL merged because of a pipe; validators must run bare or under pipefail | process; experts: BASH_TOOLS/validator-invocation rule; check: CI never pipes validator output | open (check candidate) |
| L-15 | 07-15 | env-pinning | A hardcoded runtime path in one harness script (Node v24 in supervise.sh) ABI-broke a native module for every other consumer of the repo; runtimes resolve from pinned files (.nvmrc) with boot assertions | harness (fixed, W3-15); experts: SESSION_PRIMER env-assertion pattern | shipped |
| L-16 | 07-15 | harness-robustness | Crash-cleanup that deletes sw/* branches also deleted blocked tickets' "Branch kept" evidence — cleanup globs must exclude evidence namespaces | harness (fixed: blocked/<id> rename) | shipped |
| L-17 | 07-15 | observability | Per-session output buffered until session end = 20-min blind spots; liveness lives in worktree commits — the product's heartbeat events (FR-C4/§7.1) are the real fix | product: W3-02/W4-03 (ticketed); harness: watch loop documented | ticketed |
| L-18 | 07-15 | model-economics | Sonnet floor confirmed (amplifier LESSONS 25 adopted: haiku ≤1pt only); frontier-off-by-default (no --escalate) = the escalation-token principle by configuration — hard tickets park for a human instead of silently spending | harness (shipped); product: D-018 modes | shipped |
| L-19 | 07-15 | scripts | A zero-dep script with no arg parsing executed a full import when probed with --help; scripts must no-op on unknown flags (benign here — it WAS the needed resync — but the class is dangerous) | experts: script-authoring rule (arg-parse or refuse); harness: open | open (check candidate) |
| L-20 | 07-14 | review-method | Running the product's own validators against its own board/process finds what reading cannot (66 lane violations; FR-T6/FR-N1 had no owner) — dogfood-as-review is a repeatable pass | process; product: W8-01 dogfood gate already; experts: RALPH loop VERIFY step "run the subject's own validators on itself" | open (process rule) |
| L-21 | 07-15 | run-ops | One conductor per account (amplifier doctrine + our halved-throughput record); no mutex exists — coordination is discipline; a lock-file convention is a cheap harness upgrade | harness: open (lockfile); product: FR-F3 global governor covers it properly | open |
| L-22 | 07-15 | board-hygiene | Harness crash-restarts silently reset in_progress→retry with no per-ticket crash-attempt memory — 6 sessions burned on one ticket before a human looked; retry accounting must survive restarts | harness: open (attempt ledger in plan notes survives now that markBlocked works); product: FR-H2 session caps are per-ticket and durable | open (harness), ticketed (product) |

| L-23 | 07-15 | seams | The seam law has TWO halves and both must be scope-checked: (a) dependency declaration (consumer package.json) AND (b) the public re-export barrel (producer index.ts) — W3-12 blocked a third time because the first scope fix covered (a) only; deep imports were rightly lint-forbidden by W3-10, making (b) mandatory | product: W5-07 seam-linter checks BOTH; experts: validate-tickets heuristic covers exporting barrel | open (ticketed via W5-07) |
| L-24 | 07-15 | honest-blocks | A disciplined block note (root cause + exact fix + why workarounds fail + "do not retry without X") converts a dead ticket into a 2-minute human fix — the agent's W3-12 note named the one-line barrel export and the lint rule forbidding the alternative; zero wasted retries after. Reward the pattern: blocked-with-evidence is a SUCCESS state of the discipline | experts: BOUNDED_TASK_CONTRACT block-note template (root-cause/fix/escalation fields); product: blocked-card UI renders these fields structured | open (both) |

| L-25 | 07-15 | board-states | What looked like a session dying at the finish line was a DELIBERATE honest partial: the agent split unmeetable criteria into follow-up tickets (W3-17 + a W3-01b amendment) and left the ticket in_progress with a full narrative — but the harness state machine (done\|blocked only) can't express "partial with owned remainder," so it blocked the ticket and nearly caused a redo. Verify the maker's committed record before narrating a failure | product: coverage PARTIAL is first-class (FR-L1/L4) + Harbormaster resume re-verifies instead of redoing (FR-H3, ticketed); harness: open (treat in_progress+split-notes as park-with-evidence) | ticketed (product) |
| L-26 | 07-15 | observability | Review findings were logged as COUNTS only ("newHigh=3") — the finding text lived only in the retry prompt and died with it; post-hoc adjudication was impossible | harness: open (persist findings JSON per review); product: FR-L6 finding ledger with evidence history (W3-08 landed) | shipped (product) / open (harness) |
| L-27 | 07-15 | trust-recursion | **The verifier got verified**: my own close edit (flipping the maker's honest in_progress to done, plus a "human verifier" label) was caught by the permission layer and then CONTRADICTED by an independent review — maker≠verifier applies recursively to reviewers' governance edits; a status flip is code and gets the same independent review; and an agent never authors a named human signature (SC-05's blocklist principle, generalized) | process (adopted this session: status flips on agent branches land only via independent review or explicit founder text); experts: CHALLENGER/GATE_SCORING note — "the graded entity never grades itself" includes the grader | shipped (practice) / open (experts upstream) |
| L-28 | 07-15 | gate-economics | The new secrets scanner self-hits its own repo 27× (test fixtures, docs examples) — a rule that would gate-block every close the moment it's wired. Shadow-first + fixture-allowlist-before-gating proven necessary AGAIN, on the very first new rule authored after D-014 was adopted | product: D-014 (shipped); board: W3-01b allowlist acceptance added | shipped |
| L-29 | 07-15 | gate-economics | Sticky still-present findings blocked a ticket whose review verdict was APPROVE, where the findings target PRE-EXISTING code the diff never touched — sticky bookkeeping must be diff-scoped and severity-aware, and FP adjudication needs the justified-suppression path (D-014), not immortal memory | harness: open (still-present blocks only for findings inside the ticket's diff); product: D-014 suppression + FR-L6 (landed W3-08) | ticketed (product) / open (harness) |

| L-30 | 07-15 | run-ops | A merge conflict at landing FATALed the conductor, and the crash-cleanup then deleted the finished, REVIEWED branch — the ticket was silently redone from scratch (~45 min re-spend; old tip recovered from unreachable objects). Two rules: (a) landing failures park-with-preserved-branch, never fatal (harness fixed); (b) the operator does NOT commit board files (plan.json/STATUS) to main while an agent session is in flight — my mid-run landings caused the conflict. Batch board edits between landings | harness (fixed); process (operator discipline, adopted); product: serialized landing through the review queue (FR-H5) makes this structurally impossible | shipped |

| L-31 | 07-15 | run-ops | The interactive session's stop-hook (`npm test` in cwd) fired while the conductor was merging a branch in the SAME working tree — transient red on a half-merged checkout (unreproducible minutes later: 839 green under both npm and pnpm). Same as amplifier LESSONS 30. When an executor mutates a repo root, other processes must not sample it for truth mid-landing; interactive babysitting sessions should tolerate transient reds by re-running before diagnosing | process (adopted: re-run before diagnosing any hook/CI red during an active run); harness: open (park interactive cwd outside the mutated root, or hook retries once); product: worktree-isolated landings + serialized review queue make the window structural-zero | shipped (process) |

| L-32 | 07-15 | board-schema | Per-acceptance-criterion dependency closure: W4-02 bundled a Fleet-UI card (buildable) with a gateway-fairness engine test whose capabilities live in tickets NOT in depends_on — undeliverable by construction; the agent proved it empirically (require.resolve fails, stubs confirmed) and refused to fabricate, naming the exact plan-graph fix. Every AC's required capabilities must be inside the ticket's dependency closure — the challenger checked deliverable OWNERSHIP but not per-AC capability deps | experts: challenger mandate + spec-review check ("for each AC: which landed ticket provides its inputs?"); product: W5-07 decomposer emits per-AC dep proofs; harness: validate-plan heuristic candidate (AC cites FR whose wave > all dep waves → warn) | open (checks) / shipped (board fix W4-13) |

| L-33 | 07-15 | seams | The seam law's THIRD flavor: app-shell registration files (App.tsx route mount, server.ts route registration, e2e spec dirs) — a UI feature ticket cannot wire itself into the shell it must appear in; W4-02 went out-of-scope on exactly these files and the trust boundary correctly blocked it. Seam checklist is now: (a) consumer package.json deps, (b) producer barrel exports, (c) shell/registration wiring files — a feature ticket's scope must cover all three or name the owning ticket | product: W5-07 decomposer scopes registration seams per feature surface; experts: seam checklist in TICKET_SCHEMA/spec-review; board: wiring globs added to all ui-lane tickets (same lane ⇒ no D-015 violation) | shipped (board) / open (checks) |
| L-34 | 07-15 | harness-robustness | Evidence-branch rename collides on RE-block (blocked/w4-02 existed from attempt 1, so attempt 2's branch silently stayed in the sw/ kill-zone) — evidence preservation needs unique names per incident, not per ticket | harness (fixed: timestamp suffix on collision) | shipped |

| L-35 | 07-15 | harness-robustness | Session-spawned background processes outlive the session: a W4-01 agent started a dev server (tsx standalone.ts + esbuild) that ran 7.5h after its worktree was DELETED — port-squatting flake risk for every later e2e. Killing the agent process is not killing the session; the whole process TREE must go | harness: open (spawn sessions in their own process group, kill -PGID at session end); product: FR-H2 already specs "terminate the session tree" — implementation must honor it literally | open (harness) / ticketed (product) |

| L-36 | 07-15 | seams | Growth-hotspot files: a shared registration file that every feature ticket appends to (server.ts route wiring) inevitably crosses the file-size law — and the mandated split target (a server/ directory) was outside every ticket's scope, so the hard gate became unsatisfiable by construction. Scopes for always-appended files must include their split destination, or the decomposer pre-splits registration per feature from day one | product: W5-07 decomposer (registration-per-feature pattern); board: ui scopes += server/**; experts: CODE_BOOK + TICKET_SCHEMA note | shipped (board) / ticketed (product) |

| L-37 | 07-15 | trust-recursion | The W4-03 maker found a REAL trust bug: the board UI fires claim→start→close→accept all as one `local-operator` identity, so `accept` trips SELF_ACCEPT (C-4) on every ticket — the accept affordance is dead by construction. I tried to fix it by minting a `dokima-reviewer` machine identity that auto-fires accept — the permission layer BLOCKED it as manufacturing a rubber-stamp to bypass C-4, and it was right: a distinct identity with no real review behind it is maker≠verifier theater. This is a founder decision (touches locked C-4 + pending slate P-002), NOT a mechanical fix. Escalated to Brad, ticket parked. The lesson: a hard-gate that "can never pass" is sometimes the gate correctly refusing a design that isn't decided yet — resolve the design, don't engineer around the gate | product: P-002 slate needs deciding before W4-03 lands; experts: "a gate that can't pass may be an undecided design, not a bug" | open (founder decision) |

| L-38 | 07-16 | gate-economics | Layered security surfaces defeat encode-and-redo: W4-05's path safety peeled lexical `..` → dot-prefix → symlink-follow across three review passes, each maker correctly finding the next layer down. Encoding "the fix" as acceptance just surfaces the deeper layer next pass. Security-load-bearing endpoints (path reads, consent gates) need a focused human-attention pass or human-pair flag — like trust-core (F2) — not autonomous encode-redo. Signal to watch: a ticket re-blocking on a DIFFERENT (deeper) finding each pass, not the same one | product: security-sensitive endpoints get a human-pair/careful-pass class in the decomposer; process: batch layered-security stalls, don't encode-redo them; board: W4-05/06 parked for morning security pass | open |
| L-39 | 07-16 | board-schema | Acceptance-criteria overflow: W4-06 accreted to 7 criteria (>6 cap) from repeated amendments — the validator caught it, and it was a real signal the ticket is overloaded (settings matrix + autonomy + budget + berths + scopes + MCP + validator-packs + expert-overrides + rule-lifecycle + escalation-policy + consent gate — one ticket). Amendments should consolidate or split, never just append past the cap | product: W5-07 decomposer sizes tickets by acceptance-count; process: on the 6th criterion, split or merge | shipped (consolidated) |

| L-40 | 07-16 | run-ops | A ticket that adds a workspace dependency (W4-09: events→shared, gateway→events) gates green in its own worktree (install ran there) but leaves ROOT's node_modules stale after the merge — the stop-hook's `npm test` on ROOT then fails with "Cannot find package @dokima/*". Code was correct; only the link was missing. The conductor's land() must re-install on ROOT when a merge touches any package.json | harness (fixed: land() runs install on package.json-touching merges); product: worktree-per-ticket + serialized landing already isolate this, but a post-land install step belongs in the Harbormaster too | shipped (harness) |
| L-41 | 08-20 | reader-rot | A reader wired to a store nothing writes stays green forever: the roster read roleMatrix.* keys (real matrix landed elsewhere), Chat served a hardcoded empty stream ("no producer exists yet" — stale premise), the tour harness booted a main.ts whose self-boot had moved. Comments asserting "X does not exist yet" are time bombs — re-verify them when X lands | product: W13-57/W13-63 (shipped); experts: comment-premise check candidate — flag "does not exist yet"/"not wired yet" comments older than N waves | shipped |
| L-42 | 08-20 | guards-catch-authors | Repo guards caught THEIR OWN AUTHOR five times in one day (P8 scope overlap ×3, one-primary hierarchy, a11y contrast ×2, 400-line cap ×6, export ratchet on index.ts naming) — the guard system generalizes to agent authors, and each catch was a real defect, not friction | process: trust the refusal, fix the cause, never work around; experts: upstream the P8/ratchet patterns | shipped (this arc) |
| L-43 | 08-20 | verified-citations | Model findings become safe at ANY model tier when every claim must carry a citation code re-greps before it becomes work: local qwen fabricated 2/4 (dropped mechanically), five orchestrated reviewers held 24/24. The verifier layer, not the model, is the safety property | product: W13-55 ux-audit loop (shipped); experts: citation-contract for all reviewer outputs | shipped |
| L-44 | 08-20 | volatile-user-data | ONE hardcoded `/tmp` literal caused observed data loss: the wizard's sample lived in /tmp and the e2e suite glob-deleted every /tmp/dokima-sample-* on the machine — a real walkthrough's first project, gone. Product data never goes to /tmp; suites only delete what they created; harnesses pin DOKIMA_WORKSPACE_ROOT | product: W13-64 + validate-volatile-paths.mjs (shipped); process: every harness env-pins its sandbox | shipped |
| L-45 | 08-20 | outcome-visibility | A run whose outcome is invisible reads as "finished and nothing happened": park released to Ready unmarked while its comment claimed "auto-blocked" (two systems, two stories), the banner said "finished" over 0-landed, Chat stayed silent through a real session. Every terminal state needs a visible mark, honest words, and a feed entry | product: W13-63 (shipped), W13-60/61 open; experts: end-state visibility check in UAT rubric | shipped |
| L-46 | 08-28 | buried-machinery | **Real capability with no live caller is the product's most common defect class, and it is invisible to every test.** Eight instances found in one session: `runPhaseGate` wired to a route no UI calls; "Onboard existing repo" registers a folder and never invokes `runOnboardAnalysis`; `requestTimeoutMs` stored, validated and threaded but dropped by the two LOCAL provider kinds; `streamIdleMs` governing every streamed phase with no wire surface; 85 shipped validators of which 2 ran. Each passed its own tests — nothing asserts that a capability is REACHED | product: W21-95/96/97; experts: check candidate — "every exported capability names its live caller or is marked unreached"; process: the no-caller sweep is a standing pass, not a one-off | ticketed |
| L-47 | 08-28 | ungated-checks | **A check that gates nothing decays into a check nobody can trust — three instances in one session.** The capture tour rotted for a whole wave (two shipped UI changes it never learned about); `semgrep-full-audit.sh` exited 0 while scanning ZERO files for an unknown period; `validate-plan` sat RED at 53 violations while six validators gated nothing. The failure is never the check — it is that no gate calls it | product: W21-92 (tour contract in e2e), `pnpm validate` now in Law 3; experts: rule — a check lands WITH its gate or it does not land | shipped |
| L-48 | 08-28 | error-classification | **A handler that recognises only its own error class turns every other failure into a stack trace — the same bug three times.** `LaneScopeError` escaping `reportVerbError` (W21-81), a native `parseArgs` TypeError escaping `runCli` (W21-91), `SqliteError` escaping both (W21-99). Each fix taught one more type to one more handler; the durable fix is at the top-level catch, where a refusal is the default and a stack trace the exception. **The three notes naming `cli/index.ts` as the site were all wrong** — that file is a W10-74 orphan with no importers; the catch users actually hit is `bootstrap/main.ts`. A FOURTH instance (a `SqliteError` from `verify-chain` against a non-database file) was then caught by the class fix rather than by a fourth ticket | product: W22-01 (`bootstrap/fatal.ts`; `DOKIMA_STACK=1` restores the stack), W22-05 deletes the orphan; experts: CLI-authoring rule "the top-level catch refuses; stacks are opt-in" | shipped |
| L-49 | 08-28 | instrument-trust | **The instrument lied more often than the product did, and every lie looked like a product defect.** Six probe bugs in one session: a PUT to `projects/null`; a leaked server from a crashed run serving stale state; an unauthenticated 401 that left a ceiling unraised while the retry "timed out again" exactly as it would have anyway; a regex matching `Tickets drafted` (a PENDING checklist label); another matching `receipts` in the board's own static explanation; a page-load race. Also 0% CPU + no connections read as "hung" when the work had FINISHED. Product was correct in all six | process: verify the instrument before believing its verdict; watch STATE (a stage-owned control, a status line) never WORDS; measure DURING not after; read the failed-response list before believing a timeout | shipped (practice) |
| L-50 | 08-28 | settable-and-inert | **A field can be stored, validated, threaded and still never reach the thing it governs — "settable and inert" recurs one layer up each time it is fixed.** W10-57 added `requestTimeoutMs` to the registry; `providers-wire.ts` omitted it from BOTH directions, which that module's own header names as the failure mode; once wired, `providerForConfig` dropped it for exactly the two LOCAL kinds it exists for. Verified end-to-end only by setting the value over HTTP and watching the run still die at the old ceiling | product: W21-96; experts: check candidate — a settings field ships with a test that asserts it reaches the CALL, not merely the store | shipped |
| L-51 | 08-28 | stacked-limits | **Four independent 300-second ceilings sat in one request path, and only the last one mattered.** The registry timeout never reached the wire; the local provider kinds dropped it; Node's own `fetch` `headersTimeout` killed any non-streaming call at 300s regardless of configuration; and blocking calls cannot outlive it, so no setting could help. Raising the configured ceiling to 20 then 30 minutes changed nothing, three times — the diagnosis only closed after fixing the ERROR MESSAGE that was flattening its own cause | product: streaming switch + first-chunk bound (RUN_LIMITS.md updated); experts: rule — when a limit does not respond to configuration, the limit you are configuring is not the one firing | shipped |
| L-52 | 08-28 | own-discipline | **The product held itself to phases, gates, validators and maker≠verifier, and put NONE of it into the plans it wrote for users.** A generated plan was nine feature tickets that provisioned VAPID keys and an authenticated cron endpoint with nothing reviewing either; the close gate ran 2 of 85 validators. The target user has no development experience and will never think to ask for a security review — which is the argument for the product planning one | product: W21-97 (quality tickets by default + generated-project validator floor); experts: rule — a tool that enforces a discipline on itself owes the same discipline to its output | shipped |
| L-53 | 08-28 | false-findings | **Reading the source before filing killed two findings that looked certain from evidence alone.** "The wizard defaults to Ollama" — it is `useState('lm-studio')`; `innerText` prints every `<option>`, so a closed dropdown reads wrong in a dump. "Pending steps use past-tense labels" — they render `{done ? '✓' : '…'}` with opacity and weight, three signals, not colour alone. Both would have reached the board as real work | process: a comprehension finding is not filed until the render path is read; experts: CHALLENGER rule — re-grep the mechanism, not just the symptom | shipped (practice) |
| L-54 | 08-28 | scope-precheck | **Reading a ticket's acceptance against its own write_scope before claiming caught six unworkable tickets.** W21-89's criterion needed `apps/web/src/settings/**` against a server-only scope; W21-90's Decide card lives in `apps/web/src/decisions/`; W21-93 declared `catalog/**` when the data originates in the provider adapter; W21-97 named a file that does not exist. Two were cross-lane overlaps — the Law 1 schema bug — and `validate-plan` flagged both the moment they were written | product: filed as findings not worked around; experts: TICKET_SCHEMA — acceptance and write_scope are checked against each other at FILING time, not at claim | open (check candidate) |
| L-55 | 08-28 | test-blindness | **e2e caught two defects unit tests structurally could not, and both times the unit tests were mine.** `{ ...provider }` spreads a CLASS INSTANCE — own properties copied, prototype methods dropped — so `chat` became undefined while all six unit tests passed, because they only exercised `listModels`, the one method the spread defined. Proven by stashing: 74 e2e pass without the change, 73/1 with it. Three test fixtures also had to learn SSE, or the suite would have stayed green while testing a path production no longer took | process: when a change alters a transport or a shape, the fixtures move WITH it or the suite proves nothing; experts: "a green suite would prove nothing" is a reviewable claim | shipped |
| L-56 | 08-28 | measure-before-gating | **Twice in one session a proposed mechanical check was measured before being wired to an exit code, and twice the measurement changed the design.** The acceptance-vs-scope check (W22-03) fires 44 times on tickets that shipped fine — 1 genuine in a hand-sampled 25, ~4% precision — so it prints like P9's wave report and never touches the exit code. The missing-test-twin check (W22-09) fires 420 times on done tickets, 204 of them where the twin exists on disk, and was NOT BUILT: `W3-12` omits `glob.test.ts`, `W4-02` omits `server.test.ts`, both shipped, and this repo's chapter pattern means a source file often has no twin by design. A validator wrong six times out of seven teaches people to ignore it (D-014, W21-38), and a second noisy report devalues the first | process: measure precision across the whole board BEFORE choosing report-vs-fail, and close with a finding when the number says don't build (the W12-22 precedent); experts: proposing a check without a precision estimate is an incomplete proposal | shipped |
| L-57 | 08-29 | deferred-and-dropped | **Work deferred in a ticket note is work lost — five times in one session, every one written down honestly and filed nowhere.** Cross-run gate evidence (W21-73), deliverable tickets past phase 0 (W21-76), the e2e teardown retry (W21-77), the Decide-card half of the rejection notice (W21-90), the first real `@unreached` marker (W22-02). Each was recorded in the right place at the right moment, and recording it was the LAST thing that happened to it. The same session's hand-written closing summary also invented a ticket that did not exist and omitted one that did — because it was recalled rather than read off the board | process: a deferral names the ticket that carries it, or it is not a deferral; a closing summary is READ FROM `validate-plan`'s inventory, never recalled; product: P13 reports a deferral with no open carrier (measured ~50% precision, so it reports rather than fails), and `validate-plan` already prints the open set every gate run | shipped |

## Details worth keeping (evidence pointers)

- **L-05**: challenge report in session history 2026-07-14; orphans included the a11y CI gate, ⌘K palette, `runs` table, OpenAPI/SSE/idempotency, G-25. All closed same-day (+W3-15/16, W4-11/12, 12 acceptance edits).
- **L-11/L-16**: docs/work/conductor.out lines 590–640 (launches 10–16); `conductor.fatal — row.notes.push is not a function` every ~20 min; supervise cleanup regex `^(sw/|feat/w[0-9].*-auto$)`.
- **L-12**: W3-12 worktree attempts blocked with no persisted reason (destroyed by L-11); root cause proven by dependency inspection: git/loop/tickets had no @dokima/shared in package.json and the files were outside write_scope.
- **L-14**: commit cd6b7fd landed with a red F1 because `node scripts/validate-traceability.mjs | tail -1 && git commit` reports tail's exit code, not the validator's.
- **L-20**: `node --experimental-strip-types` invocation of `findLaneScopeViolations` against plan.json, 2026-07-14 — the single highest-leverage finding of the review (G-1 → D-015).

## Analysis queue (explicit asks for the next improvement pass)

1. Check candidates to build: L-03 (all-of-the-set), L-06 (Lands-claims vs board), L-10 (done-ticket acceptance edits), L-14 (pipefail in CI validator jobs), L-19 (script arg guards).
2. Upstream PR candidates to bpm-opencode-experts: L-07 (territory-release plan discipline), L-12 (seam heuristic in validate-tickets), L-14 (validator-invocation rule), L-19 (script authoring rule), L-20 (RALPH VERIFY runs the subject's own validators).
3. Product features already carrying a lesson: D-014 (L-01), D-015 (L-07), D-016 (reports-rot), D-018 (L-18), D-019 (L-09), W3-08 (L-02), W5-07 (L-04/L-12), W3-02+W4-03 heartbeats (L-17), FR-H2 durable session caps (L-22).

## L-41 — Encoding a CRITICAL security fix as future-acceptance leaves the vuln LIVE; the security gate correctly halts

**Route to:** product + process. **Status:** confirmed 2026-07-18.

When triaging blocked security tickets, I encoded precise fix directions into each ticket's *acceptance criteria* (for a later supervised build) and reset the tickets to `todo`. For HIGH findings this is fine — they're queued and non-halting. But for the **CRITICAL** partial-event-log-commit in `packages/harbormaster/src/resume.ts` (W3-03), the actual vulnerable code stayed unchanged on `main`. The conductor's wave-security-pass scans real code (not plan.json intent), correctly detected the live CRITICAL, and **halted the whole pipeline for human review** — exactly as designed (`security.critical — stopping for human review`).

**Why:** "queue a fix" ≠ "remove the vulnerability." A CRITICAL that is live on `main` blocks the pipeline regardless of any queued intent to fix it, because the trust boundary is enforced against the code that exists, not the code we plan to write.

**How to apply:** For CRITICAL findings, *build the fix promptly* (conductor, scoped to that ticket's wave so it lands before the next wave-transition security pass) rather than only encoding it as acceptance. Encode-as-acceptance is acceptable for HIGH/MEDIUM (non-halting) but a live CRITICAL must be driven to a landed fix before the pipeline can proceed. Evidence: docs/work/SECURITY_W4.md CRITICAL entry; conductor.out `security.critical` halt 2026-07-18T14:44. Related: [[maker-verifier-recursive]], the trust-boundary law (CLAUDE.md C-2/C-3).

## L-42 — Per-ticket independent review MISSED a maker-self-attest bypass; the wave-security-pass caught it (defense-in-depth validated)

**Route to:** product + experts + harness. **Status:** confirmed 2026-07-18.

W6-03 (Forge mirror reconciliation) passed its per-ticket independent review and auto-merged, but its `hasReceiptComment()` graded a ticket VERIFIED from comment TEXT alone (`body.includes(ticketId) && /receipt|verify|exit/i.test(body)`) with no author-identity check. Because the maker's own `evidence` verb posts comments under the maker identity, the maker could forge "receipt for W6-03, verify exit 0" and defeat the entire reconciliation audit — a textbook C-4/SC-03 maker-self-attests bypass, and a direct violation of the "never grep for completion strings" law. The per-ticket reviewer approved it; the **wave-security-pass** (deeper, cross-cutting) flagged it CRITICAL and halted the pipeline.

**Why:** per-ticket review is scoped to the diff and can rubber-stamp a plausible-looking verification helper; it does not always reason about the *trust model* (who can author the thing being checked). The wave-security-pass, reasoning about the whole surface, is the backstop — and it worked.

**How to apply:** (1) Add a validator/lint rule that flags completion/verification decisions made by string/regex matching on free-text (`/receipt|verify|exit|done|pass/i` over comment/body fields) — this antipattern is a recurring maker≠verifier hole. (2) Any "is this verified?" predicate must key on identity + a non-spoofable anchor (hash-chained receipt), never text. (3) Keep the two-layer defense (per-ticket review + wave-security-pass) — this incident is the case for it. Evidence: docs/work/SECURITY_W6.md CRITICAL; fix ticket W6-08. Related: [[L-41]], trust-boundary law C-4/SC-03.
