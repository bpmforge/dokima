# Dokima — Risk Register

Traces to: `docs/BLUEPRINT.md` (§2.2, §3.3, §3.9, §10, §12) and founder decisions
D-001, D-007, D-008. Owner-wave = where the mitigation must land (SCOPE.md waves).
Likelihood/Impact: L/M/H.

| ID | Risk | Likelihood | Impact | Mitigation | Owner-wave |
|----|------|-----------|--------|------------|-----------|
| R-1 | **Local-model quality variance** — a user assigns a local model a role it can't hold (e.g. challenger), gets garbage, blames the platform. The #1 predicted new-user failure mode. | H | H | **Model fitness check** (S-11): planted-defect harness gates matrix assignment with a per-(model, role) fitness card; escalation ladder R0–R4 turns residual weakness into a ledgered escalation, not a silent failure; soft-gate waivers visible (⚠) on doc phases only — build/verify gates never soften (FR-G5); guided sample project (S-17) sets expectations on the user's own hardware. | W2 (fitness), W4 (sample) |
| R-2 | **Cost runaway** — an unattended overnight run (berths N, D-010) burns frontier spend on a stuck ticket or a retry storm. | M | H | Budget breakers 70/85/100% per run + per project, aggregated across berths, hard stop at ticket boundary (S-10); per-ticket session cap ~2 → auto-blocked with evidence; escalation is evidence-triggered only, and R4 = park, never retry-forever; dry-run cost estimate before autorun (S-22). | W2, W3 |
| R-3 | **Security of agent-executed code** — agent-generated code runs on the user's machine; a malicious or confused agent exfiltrates secrets or damages the system. | M | H | C-2 trust boundary: sessions untrusted, write-scope enforced by diff, reviewer/forge tokens never enter sessions; sandbox with no network by default, container profile optional (S-25); MCP tools behind per-role allowlists + requiresApproval (S-24); secrets vault + redaction + scanner validator (S-28); NEVER-AUTO list immutable (C-5); Dokima's own threat model is a W8 deliverable. | W0 (boundary), W6, W8 |
| R-4 | **Name collision (D-001)** — CNCF dokima.io (container image builds) creates confusion, SEO conflict, or trademark friction at public launch. | M | M | Founder-acknowledged in D-001: distinct domain, but a naming/branding pass (`dokima.dev`-style or qualifier such as "Dokima Studio") is a **pre-public-launch gate** (SCOPE.md S-43); verify trademark/namespace posture in the Phase 0 research lane (COMPETITIVE_ANALYSIS.md open tasks). | Pre-launch |
| R-5 | **Forge API drift** — GitHub/Gitea API or auth changes break the mirror, branch protection setup, or PR lifecycle mid-run. | M | M | Forge mirror is optional and offline-tolerant by design (verbs queue in ticket `history[]`, flush + two-way reconciliation audit, FR-T5) — native board remains source of truth (D-004); adapters are a documented plug-in surface (NFR-5); parity validator catches silent drift; Copilot/Vertex auth flows (D-007) get first-run smoke tests since employer-provisioned credentials vary. | W6 |
| R-6 | **CSP/packaging complexity** — "one install, anyone can use" (npx + local server + SPA + child-process sessions + optional containers) fractures across macOS/Linux/WSL; onboarding failures kill adoption at minute one. | M | H | Zero-dependency default path (process sandbox, no Docker required); packaged binaries deferred until npx path is proven; WSL is a *tested* tier-1 path at 1.0 (D-009, NFR-7); guided first fifteen minutes doubles as the install smoke test (S-17); Apple-Silicon + LM Studio is a first-class tested combo (NFR-7). | W8 |
| R-7 | **Single-maintainer bus factor** — one founder holds the design context; a stall halts the product. | H | M | The product mitigates itself: full SDLC docs + DECISIONS ledger + receipts make state reconstructible; D-006 open content + D-008 standalone repo (no private umbilical) keep the project forkable; conformance suite from source-system fixtures (BLUEPRINT §11.4) encodes intended behavior as tests, not tribal knowledge. | Continuous |
| R-8 | **Adoption vs incumbents** — users default to Cursor/Copilot habits or a well-funded competitor ships receipts; Dokima stays niche. | M | H | Wedge discipline (COMPETITIVE_ANALYSIS.md): compete on trust + economics + program, never on the editor (N-1); P4/P1 beachhead personas first (USER_PERSONAS.md); open expert content as the flywheel (D-006, signed packs); onboarding-is-the-product (S-17); board export/import guarantees no lock-in (S-42) — itself an adoption argument. | W4–W8, GTM |
| R-9 | **Trust-model erosion by convenience** — waivers, soft gates, and `auto` mode accrete until receipts are theater (the gate-integrity failure class the source systems already lived through). | M | H | Waiver receipts require a human signature with agent identities rejected (FR-P2); NEVER-AUTO list immutable in-product (C-5); autonomy ledger machine-parseable and runtime-validated (FR-N3); receipt re-validation on every phase advance (input hash + validator currency); SKIPPED/WAIVED permanently visible (NFR-6). | W0, W3, W5 |
| R-10 | **SQLite single-writer ceiling** — event-log writer becomes a bottleneck or corruption point under berths-N concurrency (C-6). | L | M | Single-writer is a deliberate constraint, not an accident: all writes serialize through the core (agents never touch the DB); WAL mode + persist-before-execute + orphan sweep on boot (NFR-3); projection lag budget <1s (NFR-2) monitored; berths write via verbs/events, so contention is bounded by ticket cadence, not token throughput. | W0, W3 |

## Review cadence

Revisit at every phase gate; any new HIGH-impact risk requires a mitigation row
before the gate passes. Tickets that implement a mitigation cite the R-x ID.
