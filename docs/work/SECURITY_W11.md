# Security pass — wave W11 (2026-08-05T20:53:50.558Z)

```json
{
  "critical": [
    {
      "file": "packages/harbormaster/src/agent-session/fs-tools.ts",
      "issue": "Verified against the blocked/w11-02 branch (not yet merged to main): assertRealWithinWorktree/realpathOfNearestAncestor (~lines 98-124) only fs.realpath()'s the nearest EXISTING ANCESTOR directory of a write/edit target, never the target leaf itself. realpathOfNearestAncestor starts at path.dirname(absPath), so if the leaf component (e.g. worktree/evil.txt) is itself a pre-existing symlink pointing outside the worktree, fs.realpath() succeeds trivially on the parent, the leaf is reassembled unresolved via path.join, containment passes, and the subsequent fs.writeFile/fs.readFile follows the symlink (Node's default, no O_NOFOLLOW) and reads/writes outside the sandboxed worktree. This is exactly the CWE-59 symlink-following gap the conductor's own review flagged (plan.json note, W11-02, 2026-08-05T20:49:10Z) and it is the reason the ticket is correctly blocked rather than landed — confirmed live in the code, not just in the note. This is the core containment mechanism for agent-session file writes (the trust boundary: agent-session tool calls are untrusted and must not mutate state outside their worktree), so an escape here is a full sandbox breakout for any code path that can plant or inherit a leaf symlink (checked-in git history, a prior session's artifact, etc.).",
      "fix": "Before falling back to nearest-ancestor resolution, attempt fs.realpath() on the full target path first; if it resolves (leaf exists, symlink or not), use that as `real` and check containment against it. Only fall back to realpathOfNearestAncestor when the leaf itself does not exist (the write-new-file case). Add the red fixture the conductor's own gap description specifies (pre-existing leaf symlink pointing outside the worktree) to fs-tools.test.ts before W11-02 is unblocked."
    }
  ],
  "high": [
    {
      "file": "conductor.config.json / scripts/conductor/gates.mjs",
      "issue": "conductor.config.json's alwaysOk includes packages/events/migrations/** (added 2026-07-16 per the $alwaysOkNote, and is exactly the mechanism W10-68's notes in this diff reason about at length). gates.mjs:31 enforces it with pure path-glob matching against `git diff --name-only main...branch` (`ALWAYS_OK.some(r => r.test(f))`), with no distinction between a file being ADDED vs an EXISTING file being MODIFIED. That means any ticket's agent session — regardless of its declared write_scope — can edit an already-shipped numbered migration file in place without ever tripping the 'out-of-scope edits' gap. This directly undermines the numbered-file/append-only schema-authority invariant this same wave's tickets (W10-68) depend on for correctness (in-place edits to an existing migration silently don't re-apply to any state.db that already ran it, per W10-68's own notes) — the gate that should catch scope escapes has a blanket hole exactly where a scope escape would be most damaging.",
      "fix": "Narrow the migrations alwaysOk exemption to newly-added files only (check git diff --name-status and only exempt status 'A' under packages/events/migrations/**, still flagging 'M'/'D' on existing numbered files as out-of-scope), or add a dedicated check that any changed file under packages/events/migrations/** matching an EXISTING committed filename requires that path to be explicitly in write_scope."
    }
  ],
  "medium": [
    {
      "file": "scripts/validate-traceability.mjs:54 (interacting with scripts/conductor/land.mjs:70-71 and this diff's W11-02 entry)",
      "issue": "ticketText() computes `typeof t.notes === 'string' ? t.notes : ''` — if notes is an array it is treated as empty for traceability purposes (F1 missing-doc-path and F2 undefined-FR/NFR-citation checks). land.mjs:70-71 unconditionally normalizes `row.notes` to an array the first time any ticket is blocked (`if (!Array.isArray(row.notes)) row.notes = ...; row.notes.push(...)`). This diff's W11-02 entry is a live instance: its notes field changed from a string to a 2-element array in this exact commit, so from this point on validate-traceability.mjs silently stops scanning ANY of that ticket's notes content (including the pre-existing text) for doc-path and FR/NFR citations. Since every ticket that gets blocked (the conductor's normal escalation path) will hit this same conversion, this is a systemic, silent weakening of a requirements-traceability gate, not a one-off.",
      "fix": "Fix ticketText() to handle both shapes: `(Array.isArray(t.notes) ? t.notes.join('\\n') : typeof t.notes === 'string' ? t.notes : '')`."
    }
  ],
  "notes": "This wave's diff is almost entirely plan.json ticket metadata (status/write_scope/notes text), not executable application code, so classic injection/deserialization/secrets checks mostly don't apply directly to the diff's own content — no hardcoded secrets, no unsafe deserialization (plan.json is read via plain JSON.parse in scripts/conductor-lib/board.mjs), no dependency/lockfile changes, and no command-construction from the new write_scope glob (packages/events/migrations/*model_matrix*, consistent with existing narrow-glob conventions and passed through the same globToRegex/escaping path as every other ticket). However, the diff's own narrative (W11-02's conductor block note) pointed directly at a real, verified sandbox-escape bug in packages/harbormaster/src/agent-session/fs-tools.ts on the not-yet-merged blocked/w11-02 branch, and investigating the trust-boundary reasoning in W10-68's notes surfaced a second real gap in the write_scope enforcement mechanism (conductor.config.json/gates.mjs) plus a third in the traceability gate (validate-traceability.mjs) that the conductor's own blocking behavior triggers. All three were confirmed by reading the actual current source, not inferred from the JSON text alone. Recommend fixing all three before W11-02 lands and before any further tickets rely on the migrations alwaysOk exemption."
}
```
