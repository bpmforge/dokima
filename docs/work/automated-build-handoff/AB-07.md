# AB-07: Execute security checks in bounded groups

Depends on: AB-06

Read START_HERE.md and the contract sections of IMPLEMENTATION_PLAN.md before this card.

## Existing files to read and expected change area

- `apps/server/src/api/pipeline/onboard-executor.ts`
- `apps/server/src/api/pipeline/onboard-executor.test.ts`
- `packages/validators/src/run.ts`

## Proposed new files

- `packages/harbormaster/src/check-scheduler.ts` (proposed; create only if no existing module owns this responsibility)
- `packages/harbormaster/src/check-scheduler.test.ts` (proposed; create only if no existing module owns this responsibility)

Include necessary package exports, production caller files and adjacent tests in the claimed scope. Reading a listed file does not require editing it.

## Do these steps in order

1. Implement the scheduling algorithm in IMPLEMENTATION_PLAN.md using injected execute callbacks. Keep model routing in apps/server; packages must not import apps.
2. Run ready independent nodes under a finite worker pool. Scanner concurrency starts at the existing validator pool limit; model requests also pass the endpoint limiter.
3. Each node reads only its completed predecessor artifacts and a fixed source snapshot. Never hand out a mutable shared cache while other workers are writing.
4. Persist started/completed/failed events as work occurs through the existing single writer. Await all started work on failure/stop before returning; no orphan promises.
5. Wire the scheduler to real onboard execution and export it for build checks. Preserve public result fields where practical. Update tests that intentionally asserted obsolete serial security order; retain dependency assertions.
6. Report complete, incomplete and failed distinctly. A failure must not mint an all-clear coverage/gate receipt or final completed plan.

## Acceptance — all must hold

- [ ] Deferred-promise tests prove independent scanner overlap and predecessor ordering without flaky wall-clock assertions.
- [ ] Source mutation, failure, thrown dispatch and stop do not yield a clean final result.
- [ ] A runtime integration test traverses the actual onboard entry point, not only the scheduler helper.

## Focused verification

```sh
pnpm exec vitest run packages/harbormaster/src/check-scheduler.test.ts apps/server/src/api/pipeline/onboard-executor.test.ts --retry=0
```

Then run the full repository gate from START_HERE.md before closing. For a documentation-only card, perform documentation/schema checks and record that no runtime implementation was changed.

## Handoff at completion

Update PROGRESS.md with ticket mapping, commit, actual commands and exit codes, test totals, evidence, remaining findings and next AB id. Do not claim the next card is implemented.
