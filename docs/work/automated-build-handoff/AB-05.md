# AB-05: Declare security dependencies and applicability

Depends on: AB-04

Read START_HERE.md and the contract sections of IMPLEMENTATION_PLAN.md before this card.

## Existing files to read and expected change area

- `packages/pipeline/src/modes/security-cluster.ts`
- `packages/pipeline/src/modes/onboard.ts`
- `packages/pipeline/src/modes/types.ts`
- `packages/pipeline/src/run/run-onboard.ts`

## Proposed new files

- `packages/pipeline/src/modes/security-plan.ts` (proposed; create only if no existing module owns this responsibility)
- `packages/pipeline/src/modes/security-plan.test.ts` (proposed; create only if no existing module owns this responsibility)

Include necessary package exports, production caller files and adjacent tests in the claimed scope. Reading a listed file does not require editing it.

## Do these steps in order

1. Keep the seven existing general onboarding steps sequential for this first release. Optimize security groups first; do not rewrite the whole six-phase product pipeline.
2. Define the exact graph in IMPLEMENTATION_PLAN.md using existing step ids. Add an exported plan representation instead of discovering hidden topology through fake execution for the new async path.
3. Classify applicability from a runtime-owned project profile and file inventory. OWASP web applies to the web-app profile; LLM checks to apps integrating LLM features; cloud/IaC to relevant deployed/configured surfaces. Unknown applicability stays required or unresolved, never silently skipped.
4. Represent a supported skip as NOT_APPLICABLE with reason and inventory digest. Do not count it as a passed scan. Synthesis must know missing and skipped categories.
5. Validate unique ids, known predecessors, no cycles, and no same-output writers in a concurrent group before any model/tool call.

## Acceptance — all must hold

- [ ] Security graph has no missing/cyclic edges; attack synthesis waits for every applicable specialist result.
- [ ] Cloud/IaC absence produces an explicit reason; unknown detection never becomes a clean security result.
- [ ] Existing seven-step onboarding output remains compatible.

## Focused verification

```sh
pnpm exec vitest run packages/pipeline/src/modes/security-plan.test.ts packages/pipeline/src/modes/security-cluster.test.ts packages/pipeline/src/run/run-onboard.test.ts --retry=0
```

Then run the full repository gate from START_HERE.md before closing. For a documentation-only card, perform documentation/schema checks and record that no runtime implementation was changed.

## Handoff at completion

Update PROGRESS.md with ticket mapping, commit, actual commands and exit codes, test totals, evidence, remaining findings and next AB id. Do not claim the next card is implemented.
