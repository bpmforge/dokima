# AB-16: Prove the complete workflow through the real entry points

Depends on: AB-15

Read START_HERE.md and the contract sections of IMPLEMENTATION_PLAN.md before this card.

## Existing files to read and expected change area

- `apps/web/e2e/fixtures/fake-model-gateway.ts`
- `apps/web/e2e/plans.spec.ts`
- `apps/web/e2e/decisions-resume.spec.ts`
- `apps/server/src/api/server/runs-routes.test.ts`

## Proposed new files

- `apps/web/e2e/approved-build.spec.ts` (proposed; create only if no existing module owns this responsibility)
- `docs/work/automated-build/ACCEPTANCE_REPORT.md` (proposed; create only if no existing module owns this responsibility)

Include necessary package exports, production caller files and adjacent tests in the claimed scope. Reading a listed file does not require editing it.

## Do these steps in order

1. Create a small Node web-app fixture with 3 dependent tickets and 1 independent ticket. Seed an insecure/failing implementation on the first maker attempt and a correct repair on the next fake response.
2. Drive the actual HTTP/UI start route, executeBuildRun, ticket engine, scanner adapters, reviewer, repair and acceptance. Fake only external model/tool processes where necessary; do not mock the orchestrator into returning done.
3. Assert dependency source inheritance, source-bound verification, machine identity separation, repair counts and grouped scanner overlap.
4. Add distinct red scenarios for same-model review, false model approval, missing scanner, exhausted repair, stale receipt, restart, duplicate start, changed approval and separate main/deploy decision.
5. Run full gates with retries disabled for the new scenarios at least once so a flaky first attempt is visible. Record actual counts and limitations, not expected counts.

## Acceptance — all must hold

- [ ] End-to-end happy path reaches verified preview with no routine intervention and no main merge.
- [ ] Every red scenario ends in the stated non-success status and preserves evidence.
- [ ] Acceptance report links real commands/results and names any untested external behavior.

## Focused verification

```sh
pnpm --filter @dokima/web exec playwright test approved-build.spec.ts --workers=1 --retries=0
```

Then run the full repository gate from START_HERE.md before closing. For a documentation-only card, perform documentation/schema checks and record that no runtime implementation was changed.

## Handoff at completion

Update PROGRESS.md with ticket mapping, commit, actual commands and exit codes, test totals, evidence, remaining findings and next AB id. Do not claim the next card is implemented.
