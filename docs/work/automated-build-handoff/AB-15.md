# AB-15: Remove duplicate developer gates without dropping coverage

Depends on: AB-14

Read START_HERE.md and the contract sections of IMPLEMENTATION_PLAN.md before this card.

## Existing files to read and expected change area

- `scripts/run-validators.mjs`
- `conductor.config.json`
- `CLAUDE.md`
- `PLAYBOOK.md`
- `package.json`

## Proposed new files

- `scripts/gate-plan.mjs` (proposed; create only if no existing module owns this responsibility)
- `scripts/gate-plan.test.mjs` (proposed; create only if no existing module owns this responsibility)
- `docs/work/automated-build/GATE_TIMINGS.md` (proposed; create only if no existing module owns this responsibility)

Include necessary package exports, production caller files and adjacent tests in the claimed scope. Reading a listed file does not require editing it.

## Do these steps in order

1. Treat this as a separate repository-development optimization, not a product scheduler change. First inspect all gate definitions in conductor.config.json and CI; list exact duplicate executions and dependency constraints.
2. Parallelize only independent read-only source validators with bounded child processes and stable grouped output. Keep aggregate exit nonzero if any child fails. Preserve configured ratchet arguments.
3. Keep validate-temp-leaks after every test process settles. Do not put it in the parallel group; it inspects machine state and would misclassify live workers.
4. Do not remove full per-ticket checks in this initial handoff. Measure affected-check selection in shadow/report-only mode first. Receipt-independent reviewer reruns are intentional until evidence-sharing receives its own trust test.
5. Update instructions to match the actual parallel command only after equivalent failure detection is demonstrated. Do not weaken ratchets, omit tests, or raise timeouts as a performance fix.

## Acceptance — all must hold

- [ ] A deliberately failing validator fails the whole gate while preserving all group output.
- [ ] Ratchet arguments match the serial baseline; temp leak check starts only after workers exit.
- [ ] Before/after timings include identical input snapshot and check set. No speedup is claimed without measurement.

## Focused verification

```sh
node --test scripts/gate-plan.test.mjs
```

Then run the full repository gate from START_HERE.md before closing. For a documentation-only card, perform documentation/schema checks and record that no runtime implementation was changed.

## Handoff at completion

Update PROGRESS.md with ticket mapping, commit, actual commands and exit codes, test totals, evidence, remaining findings and next AB id. Do not claim the next card is implemented.
