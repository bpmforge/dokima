# AB-18: Port the proven review grouping to Attest

Depends on: AB-17

Read START_HERE.md and the contract sections of IMPLEMENTATION_PLAN.md before this card.

## Existing files to read and expected change area

- Attest paths are listed in the steps below.

## Proposed new files

- Prefer the existing conductor and adjacent test files.

Include necessary package exports, production caller files and adjacent tests in the claimed scope. Reading a listed file does not require editing it.

## Do these steps in order

1. Work in /Users/bmatthews/Code/attest only after Dokima AB-16 is green. Read its own instructions and current git status first. Do not copy Dokima package imports into Attest.
2. Inspect scripts/conductor/conductor.mjs runReviewRound and its conductor tests. Capture one fixed implementation head and prepare distinct reviewer report paths before dispatch.
3. Replace the serial reviewer loop with a small bounded pool appropriate to the configured endpoint/model runner. Preserve reviewer selection, archiveStaleReview, missing-verdict refusal, independent identities and fix-before-reverify order.
4. Wait for the whole review group, consolidate results in deterministic order, then run the existing fix loop. No reviewer writes implementation; no fixes race with active reviews.
5. Add a shared budget/stop path if missing; do not multiply concurrent sessions beyond authorized capacity. Keep the original serial limit as the default for a single constrained local model.
6. Run conductor regression tests and the repository-required checks. Release/version/content-pack updates are a subsequent deliberate action, not automatic collateral work.

## Acceptance — all must hold

- [ ] Independent fake reviewer sessions overlap at limit>1 and stay serial at limit=1.
- [ ] Failure/missing report still blocks, report paths do not collide, repair waits for every started reviewer.
- [ ] Attest regression tests pass without modifying Dokima signed content.

## Focused verification

```sh
node --test scripts/conductor/conductor.test.mjs scripts/conductor/conductor.hardening.test.mjs
```

Then run the full repository gate from START_HERE.md before closing. For a documentation-only card, perform documentation/schema checks and record that no runtime implementation was changed.

## Handoff at completion

Update PROGRESS.md with ticket mapping, commit, actual commands and exit codes, test totals, evidence, remaining findings and next AB id. Do not claim the next card is implemented.
