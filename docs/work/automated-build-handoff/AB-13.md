# AB-13: Persist run state, stop requests and resumable progress

Depends on: AB-12

Read START_HERE.md and the contract sections of IMPLEMENTATION_PLAN.md before this card.

## Existing files to read and expected change area

- `apps/server/src/api/server/runs-job.ts`
- `apps/server/src/api/server/runs-routes.ts`
- `packages/harbormaster/src/breakpoints-runs.ts`
- `packages/harbormaster/src/resume.ts`

## Proposed new files

- `apps/server/src/api/server/approved-build-run-state.ts` (proposed; create only if no existing module owns this responsibility)
- `apps/server/src/api/server/approved-build-run-state.test.ts` (proposed; create only if no existing module owns this responsibility)

Include necessary package exports, production caller files and adjacent tests in the claimed scope. Reading a listed file does not require editing it.

## Do these steps in order

1. Reuse existing RunRecord/events for HTTP-started runs. The current buildRuns and stopRequests Maps may remain caches, but must not be the only source of status or stop intent.
2. Persist accepted start before dispatch. Validate run-id/project ownership and make repeated start with same id idempotent. Refuse a conflicting duplicate instead of starting a second writer.
3. Persist phase/check/repair progress and terminal outcome; distinguish completed verification, awaiting decision, stopped, failed and interrupted. Exit 0 alone is not build completion.
4. On restart mark orphan running work interrupted and offer/resume only through existing drift checks with the same approved snapshot and budgets. Do not replay publication, acceptance or completed external actions.
5. Stop prevents new tasks promptly; describe whether in-flight work drains or is cancelled. Use existing watchdog/sandbox termination for hard timeout. Close the event log only after started workers settle.

## Acceptance — all must hold

- [ ] Recreate the server/job module after a simulated crash: status remains visible and retry counters survive.
- [ ] Duplicate start does not create duplicate work; cross-project run id cannot read/control another project.
- [ ] Stop survives restart and blocks new work until explicit resume.

## Focused verification

```sh
pnpm exec vitest run apps/server/src/api/server/approved-build-run-state.test.ts apps/server/src/api/server/runs-routes.test.ts --retry=0
```

Then run the full repository gate from START_HERE.md before closing. For a documentation-only card, perform documentation/schema checks and record that no runtime implementation was changed.

## Handoff at completion

Update PROGRESS.md with ticket mapping, commit, actual commands and exit codes, test totals, evidence, remaining findings and next AB id. Do not claim the next card is implemented.
