# AB-03: Give the reviewer the actual source change

Depends on: AB-02

Read START_HERE.md and the contract sections of IMPLEMENTATION_PLAN.md before this card.

## Existing files to read and expected change area

- `packages/harbormaster/src/loop-review.ts`
- `packages/harbormaster/src/loop-review.test.ts`
- `packages/harbormaster/src/loop-gates-verify.ts`
- `packages/git/src/index.ts`

## Proposed new files

- `packages/harbormaster/src/review-evidence.ts` (proposed; create only if no existing module owns this responsibility)
- `packages/harbormaster/src/review-evidence.test.ts` (proposed; create only if no existing module owns this responsibility)

Include necessary package exports, production caller files and adjacent tests in the claimed scope. Reading a listed file does not require editing it.

## Do these steps in order

1. Build a runtime-owned review evidence bundle from the ticket worktree and validated base/head commits. Include a real diff, acceptance criteria, relevant bounded source context, independent verify result, and the source digest.
2. Resolve refs using existing git utilities and argument arrays. Do not interpolate model text into shell commands. Disable external git diff helpers/text conversion when reading untrusted repo content.
3. Redact secrets before sending data to the reviewer and before ledgering summaries. Keep byte/token limits. If relevant changes exceed the limit, mark the review incomplete; do not silently approve an omitted diff.
4. Pass the bundle to reviewPrompt. Existing prompt lists filenames/commits but no diff; fix the real function, not a second helper with no caller.
5. Bind verdict events to the reviewed head and input digest. Reject stale evidence when the worktree changes between evidence collection and decision. Preserve the independently executed verify result as authoritative.

## Acceptance — all must hold

- [ ] A fake reviewer captures a prompt containing the planted changed line, not merely its filename.
- [ ] Passing tests plus an insecure code change remain visible to the reviewer.
- [ ] Secrets are redacted; oversized/dirty/stale evidence cannot produce an eligible automated acceptance.
- [ ] A nonzero independent verify result cannot be overridden by model CONFIRMED.

## Focused verification

```sh
pnpm exec vitest run packages/harbormaster/src/review-evidence.test.ts packages/harbormaster/src/loop-review.test.ts --retry=0
```

Then run the full repository gate from START_HERE.md before closing. For a documentation-only card, perform documentation/schema checks and record that no runtime implementation was changed.

## Handoff at completion

Update PROGRESS.md with ticket mapping, commit, actual commands and exit codes, test totals, evidence, remaining findings and next AB id. Do not claim the next card is implemented.
