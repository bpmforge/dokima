# AB-01: Define the approved-run policy and pause rules

Depends on: AB-00

Read START_HERE.md and the contract sections of IMPLEMENTATION_PLAN.md before this card.

## Existing files to read and expected change area

- `packages/harbormaster/src/autonomy.ts`
- `packages/harbormaster/src/autonomy-types.ts`
- `packages/harbormaster/src/review-queue-classifier.ts`
- `packages/harbormaster/src/breakpoints-clarifications.ts`
- `docs/DECISIONS.md`

## Proposed new files

- `packages/harbormaster/src/approved-build-policy.ts` (proposed; create only if no existing module owns this responsibility)
- `packages/harbormaster/src/approved-build-policy.test.ts` (proposed; create only if no existing module owns this responsibility)
- `docs/work/automated-build/PAUSE_SITES.md` (proposed; create only if no existing module owns this responsibility)

Include necessary package exports, production caller files and adjacent tests in the claimed scope. Reading a listed file does not require editing it.

## Do these steps in order

1. Inventory actual callers with rg, not just exported helpers. In this reviewed checkout askClarification and resolvePauseAction did not have the expected production wiring. Record missing callers honestly.
2. Implement a pure decision function using the pause table in IMPLEMENTATION_PLAN.md. Inputs come from runtime-owned approved policy and action facts; never accept a model field such as safe=true as authorization.
3. Use isNeverAutoPauseSite and existing classifier. Do not change their protected sets. An ordinary bounded coding/test/repair action needs no clarification. Unclear requirements are not ordinary implementation choices.
4. Keep resolvePauseAction compatibility for old callers; put new opted-in behavior behind a versioned approved-build policy. Missing or legacy settings select the old interactive behavior.
5. Record that D-032 review is completed only for the enumerated cases. Any unenumerated pause remains human-gated. Do not blanket-dismiss existing clarification rows.

## Acceptance — all must hold

- [ ] Table-driven tests cover every pause-table row in auto and interactive modes.
- [ ] Unknown kind, forged safe flag, missing approval, changed scope, higher budget, and protected actions cannot auto-proceed.
- [ ] Routine implementation inside an approved scope can proceed without an extra approval.

## Focused verification

```sh
pnpm exec vitest run packages/harbormaster/src/approved-build-policy.test.ts packages/harbormaster/src/autonomy.test.ts packages/harbormaster/src/review-queue-classifier.test.ts --retry=0
```

Then run the full repository gate from START_HERE.md before closing. For a documentation-only card, perform documentation/schema checks and record that no runtime implementation was changed.

## Handoff at completion

Update PROGRESS.md with ticket mapping, commit, actual commands and exit codes, test totals, evidence, remaining findings and next AB id. Do not claim the next card is implemented.
