# AB-14: Expose one approval and a readable build experience

Depends on: AB-13

Read START_HERE.md and the contract sections of IMPLEMENTATION_PLAN.md before this card.

## Existing files to read and expected change area

- `apps/web/src/settings/AutonomyBudgetPanel.tsx`
- `apps/web/src/settings/AutonomyBudgetPanel.test.tsx`
- `apps/web/src/board/RunSummary.tsx`
- `apps/web/src/notifications/TicketEvidence.tsx`
- `apps/server/src/api/server/autonomy-budget-routes.ts`

## Proposed new files

- `apps/web/src/board/ApprovedBuildPanel.tsx` (proposed; create only if no existing module owns this responsibility)
- `apps/web/src/board/ApprovedBuildPanel.test.tsx` (proposed; create only if no existing module owns this responsibility)

Include necessary package exports, production caller files and adjacent tests in the claimed scope. Reading a listed file does not require editing it.

## Do these steps in order

1. Add a reachable approved-build confirmation to the existing start flow. Show design/story revision, scope, model choices, budget and what still requires a decision. It must issue the versioned approval/start request from AB-02.
2. Wire the new component into the actual board route that owns Start run; locate and include that parent file in the ticket scope before coding. An unmounted component does not satisfy this card.
3. Show Building, Checking, Fixing, Ready to review, Needs your decision and Stopped from durable state. Show one consolidated findings/decision summary with drill-down to receipts.
4. Expose Stop/Resume and a working preview or explicit preview-unavailable reason. Use the existing preview/artifact surface if present; do not embed a second IDE.
5. Update the autonomy panel copy only after the new behavior is wired. Existing auto selections require the new confirmation. Publication/main merge remains visibly separate.
6. If only one maker/reviewer model is available, explain before launch that independent machine review needs another model or human review; never advertise a fully unattended finish in that configuration.

## Acceptance — all must hold

- [ ] The real UI can start an approved run and display a build/check/fix sequence.
- [ ] Reload restores progress and decisions from server state.
- [ ] No repeated routine confirmation; protected decision remains visible; keyboard navigation works.

## Focused verification

```sh
pnpm exec vitest run apps/web/src/board/ApprovedBuildPanel.test.tsx apps/web/src/settings/AutonomyBudgetPanel.test.tsx --retry=0
```

Then run the full repository gate from START_HERE.md before closing. For a documentation-only card, perform documentation/schema checks and record that no runtime implementation was changed.

## Handoff at completion

Update PROGRESS.md with ticket mapping, commit, actual commands and exit codes, test totals, evidence, remaining findings and next AB id. Do not claim the next card is implemented.
