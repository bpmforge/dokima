# AB-08: Invalidate dependent evidence on retry

Depends on: AB-07

Read START_HERE.md and the contract sections of IMPLEMENTATION_PLAN.md before this card.

## Existing files to read and expected change area

- `apps/server/src/api/pipeline/onboard-executor.ts`
- `packages/harbormaster/src/check-scheduler.ts` (created by prerequisite AB-07; not present at the reviewed baseline)
- `packages/harbormaster/src/review-status.ts`

## Proposed new files

- `packages/harbormaster/src/check-evidence.ts` (proposed; create only if no existing module owns this responsibility)
- `packages/harbormaster/src/check-evidence.test.ts` (proposed; create only if no existing module owns this responsibility)

Include necessary package exports, production caller files and adjacent tests in the claimed scope. Reading a listed file does not require editing it.

## Do these steps in order

1. Implement canonical evidence keys from source tree, command/args, tool/rules version, configuration and predecessor result digests. Start with the whole relevant source tree rather than clever incomplete dependency hashing.
2. On a failed step retry, invalidate all transitive descendants. If a previously successful predecessor changes output, recompute its descendants too.
3. Reuse only successful runtime-owned evidence whose exact key still matches and whose artifact exists and hashes correctly. Never reuse model prose as a signed scanner pass.
4. Keep initial reuse run-scoped and optionally persisted via existing events/artifacts for restart. Defer a global cross-project cache. Missing version/digest means rerun.
5. Update reviewStatusFor so an old CONFIRMED cannot remain current after reject/new close/new source revision. Render it as stale or never-reviewed until a fresh matching verdict arrives.

## Acceptance — all must hold

- [ ] An upstream failure followed by success reruns attack-chain/threat-model descendants.
- [ ] Unchanged independent work is not repeated in the same retry round.
- [ ] Changed rules/config/source/missing output invalidates reuse; old verdict cannot authorize new code.

## Focused verification

```sh
pnpm exec vitest run packages/harbormaster/src/check-evidence.test.ts packages/harbormaster/src/review-status.test.ts apps/server/src/api/pipeline/onboard-executor.test.ts --retry=0
```

Then run the full repository gate from START_HERE.md before closing. For a documentation-only card, perform documentation/schema checks and record that no runtime implementation was changed.

## Handoff at completion

Update PROGRESS.md with ticket mapping, commit, actual commands and exit codes, test totals, evidence, remaining findings and next AB id. Do not claim the next card is implemented.
