# AB-00: Capture a clean baseline and register this work

Depends on: none

Read START_HERE.md and the contract sections of IMPLEMENTATION_PLAN.md before this card.

## Existing files to read and expected change area

- `plan.json`
- `CLAUDE.md`
- `MASTER_PROMPT.md`
- `PLAYBOOK.md`
- `package.json`
- `conductor.config.json`
- `docs/DECISIONS.md`
- `docs/TESTING.md`

## Proposed new files

- `docs/work/automated-build/BASELINE.md` (proposed; create only if no existing module owns this responsibility)
- `docs/work/automated-build/PROGRESS.md` (proposed; create only if no existing module owns this responsibility)

Include necessary package exports, production caller files and adjacent tests in the claimed scope. Reading a listed file does not require editing it.

## Do these steps in order

1. Read START_HERE.md and the contracts in IMPLEMENTATION_PLAN.md. Work in the Dokima repository, not Attest. Do not start a live model run.
2. Record git HEAD, git status, Node and pnpm versions. Use Node 22 and pnpm 11. Read the repository instructions before installing dependencies. Preserve unrelated changes.
3. Run the full gate commands listed in START_HERE.md once. Capture exit codes, duration and test totals. Record pre-existing failures separately; never call a failed baseline green.
4. Register only the next card in plan.json before implementing it. Map AB ids to unused repository ticket ids and record that mapping. For autonomy wiring, extend/split W13-32 with linked children rather than duplicate it. Include actual touched exports, test files and composition roots in scope.
5. Keep the new work out of the existing unattended conductor until its ticket mapping and first claim are correct. Claim through the normal repository process. No source changes in this card.

## Acceptance — all must hold

- [ ] BASELINE.md records the checkout and every gate outcome, including skips and failures.
- [ ] PROGRESS.md contains AB ids, repository ticket mapping, status, commits and evidence slots. Nothing is marked implemented.

## Focused verification

```sh
node scripts/validate-plan.mjs
```

Then run the full repository gate from START_HERE.md before closing. For a documentation-only card, perform documentation/schema checks and record that no runtime implementation was changed.

## Handoff at completion

Update PROGRESS.md with ticket mapping, commit, actual commands and exit codes, test totals, evidence, remaining findings and next AB id. Do not claim the next card is implemented.
