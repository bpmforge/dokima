{
  "$note": "Project config for scripts/conductor.mjs. Everything project-specific lives here — including the board's own location (boardPath) — so the conductor script is reusable across repos unchanged. Absent keys fall back to built-in defaults.",
  "branchPrefix": "sw/",
  "worktreeDir": "../.dokima-worktrees",
  "isolation": "worktree",
  "toolchainMarker": "package.json",
  "boardPath": "plan.json",
  "install": [
    "pnpm",
    [
      "install",
      "--prefer-offline"
    ]
  ],
  "gates": [
    [
      "pnpm",
      [
        "lint"
      ]
    ],
    [
      "pnpm",
      [
        "typecheck"
      ]
    ],
    [
      "pnpm",
      [
        "test"
      ]
    ],
    [
      "node",
      [
        "scripts/validate-plan.mjs"
      ]
    ],
    [
      "node",
      [
        "scripts/validate-traceability.mjs"
      ]
    ],
    [
      "node",
      [
        "scripts/validate-exports.mjs",
        "--max",
        "62",
        "--max-buried",
        "73"
      ]
    ],
    [
      "bash",
      "-lc",
      "OUT=$(mktemp -d) && node apps/web/scripts/capture-acceptance.mjs gate --out \"$OUT\" >/dev/null && node scripts/validate-visual.mjs --run \"$OUT\"; rc=$?; rm -rf \"$OUT\"; exit $rc"
    ],
    [
      "pnpm",
      [
        "--filter",
        "@dokima/web",
        "e2e"
      ]
    ]
  ],
  "gateTimeoutMin": 15,
  "testSibling": {
    "source": "\\.(ts|tsx)$",
    "test": "\\.test\\.(ts|tsx)$"
  },
  "migrationScope": {
    "trigger": "\\b(new|adds?|adding|added|creates?|creating)\\b[\\s\\S]{0,60}\\b(column|table)\\b|\\b(column|table)\\b[\\s\\S]{0,60}\\b(new|adds?|adding|added|creates?|creating)\\b",
    "dir": "^packages/events/migrations/"
  },
  "validators": {
    "dir": "content/validators",
    "gate": [
      "validate-file-size",
      "validate-circular-deps"
    ],
    "advisory": [
      "validate-code-health",
      "validate-dead-code",
      "validate-verification-integrity"
    ],
    "$note": "gate[] = reliable/objective validators, run diff-scoped as HARD gates. advisory[] = grep-heuristic validators (noisy on TS: magic-numbers-in-comments, bogus-unreachable) — their findings ANCHOR the LLM review instead of blocking, matching the source system's 'script floor + agent verified pass' design. Promote a validator to gate[] once red-fixture-calibrated. repoWide[] (W10-49): findings from these are NOT filtered to the ticket diff. Only honest while the validator reports ZERO repo-wide — then any finding is necessarily something this ticket introduced. validate-file-size hit zero at W10-48 (six over-cap files cleared across W10-46/47/48); if that count ever goes non-zero again, every ticket fails for debt it did not create, so remove the entry rather than waiving it.",
    "repoWide": [
      "validate-file-size"
    ]
  },
  "remotes": [
    "github",
    "origin"
  ],
  "alwaysOk": [
    "plan.json",
    "docs/STATUS.md",
    "docs/work/**",
    "docs/TECH_STACK.md",
    "pnpm-lock.yaml",
    "package.json",
    "pnpm-workspace.yaml",
    "tsconfig.json",
    "tsconfig.base.json",
    "vitest.workspace.ts",
    "vitest.config.ts",
    "playwright.config.ts",
    "eslint.config.js",
    ".prettierrc.json",
    ".gitignore",
    ".npmrc",
    ".nvmrc"
  ],
  "alwaysOkAddOnly": [
    "packages/events/migrations/**"
  ],
  "holdTickets": [],
  "models": {
    "maker": "sonnet",
    "cheap": "haiku",
    "reviewer": "sonnet",
    "security": "sonnet",
    "escalate": "opus",
    "cheapLanes": [
      "content"
    ],
    "cheapMaxPoints": 1
  },
  "$holdNote2": "2026-08-02 UN-HELD for an authorized overnight autonomous run (founder: \"auto work through the night\"). W10-04 was held because a Providers panel built without a spec is invention at the keyboard — W10-05 removed that risk by writing UX_SPEC 6a, which names every state, both destructive-affordance copy blocks, and the verbatim refusal strings, so W10-04 now has something to conform TO. W10-28 is a scale-adoption pass whose gate (styles.test.ts) already fails on any regression. D-018 still holds: launched WITHOUT --escalate, sonnet-only ladder. Overnight output is visually verified against screenshots before it is trusted.",
  "$holdNote": "F2 (RELEASE_TRACKER): trust-core lane needs human-pair review — the conductor never claims these unattended; Brad works them interactively. 2026-07-16: W3-01a un-held for a SUPERVISED build under Brad's explicit authorization (chose 'conductor builds, you gate merge') — conductor runs --no-merge --breakpoint ticket, PARKS the branch (never auto-merges), and Brad reviews the diff + red fixtures and authorizes the merge himself. W3-01b/c stay held until their deps merge, then get the same one-at-a-time supervised treatment.",
  "$exportsGateNote": "W12-10, 2026-08-13: validate-exports.mjs joins the per-ticket gate as a RATCHET (--max 48), not a clean-zero check. It reports exported symbols that tests reference and production code never does — something built, verified, and wired to nothing. Baseline 48 is this repo's measured debt at filing; failing every ticket for it would be the exact mistake the repoWide note above warns about, so the gate only fires when the count EXCEEDS the baseline. Lower it whenever the number drops; never raise it to make a ticket pass. CALIBRATION MATTERS HERE and was done rather than assumed: the first draft reported 529 symbols (all exports, cross-package refs only), which is instantly waivable noise. Filtering to VALUE exports via the TS checker took it to 245 (a type nobody names is harmless; a function nobody calls is dead), counting references inside the declaring package took it to 135, and counting in-file uses beyond the declaration itself — the sibling-factory false positive, e.g. createMacKeychainCredentialStore called by resolveCredentialStore beside it — took it to 48. Same shape as attest v3.5.1s honest-denominator pass (71 -> 8). What it catches today includes loop:createToolAnchor (W12-05), the three cloud provider factories, and several pipeline verbs.",
  "$barrelCountingNote": "W12-39, 2026-08-18: --max raised 43 -> 62, and this is NOT the forbidden 'raise the baseline to make a ticket pass' — the rule that forbids that is about absorbing NEW debt a change created. Nothing was wired to nothing by this ticket; the counting got sharper and 19 symbols that were already dead stopped hiding. W12-10 counted a bare word-boundary match over raw file text and said so in its own module doc ('a comment mentioning a name reads as a use'), treating false negatives as the safe direction. W12-38 then measured what that concealed and found the check's own worst instance behind two prose mentions. Comments are now blanked for both passes, and the 19 newly visible symbols are: events promoteGlobalPlaybookEntry; gateway PRESET_ROLES, BudgetBreakerTracker; harbormaster runClaimLoop, createFileStopSwitch; loop runMicroLoop, phaseGatePasses, toCoverageReportJson, createStubMemoryAnchor, updateCalibration, gateDecision; mcp getPendingApproval, decideToolCall; pipeline beginTopic, resumeTopic, skipTopic, submitAnswer, resolveFounderFork; tickets validateLaneWriteScopes. Two were hand-verified rather than trusted: every non-test reference to runClaimLoop and gateDecision is a comment, and loop-claim.ts:223's own docstring says runClaimLoop's only callers are its own tests — the sentence admitting the defect was what concealed it. The strip now lives INSIDE countReferences rather than in its caller, because as a caller-side step the guarantee held only while every future caller remembered it, and a fixture written against the function passed while proving nothing.",
  "$buriedExportsNote": "W12-38, 2026-08-18: --max-buried 73 joins the same validator as a SECOND, separate ratchet. The W12-10 pass walks package BARRELS, so it can only see what a package publishes — and the instance that exposed the hole, runEscalationPolicy (the whole D-024 option-b escalation state machine, tested, called by nothing), is not in a barrel at all, so the validator confidently printed 43 gaps and never named it. That is the WORSE shape, not a lesser one: an uncalled export is reachable and unused, while an unexported implementation cannot be adopted without a separate barrel change, which is exactly how W12-04's packer, W12-09's code index and W12-37's pinned policy each sat dormant for waves. Calibration, measured not assumed: 90 raw -> 108 once comment text stopped counting as a call (loop-land-policy.ts names runEscalationPolicy twice in prose explaining why it deliberately does NOT call it, and the barrel pass's own doc admits this over-count) -> 73 once recorded fixtures and test-helper modules were excluded, since those are exported for tests and called by nothing else BY DESIGN under law 9a. Kept as a SEPARATE number rather than folded into --max: the two have different remedies (wire it up vs. wire it up AND export it), and merging would silently move the calibrated 43. KNOWN LIMIT, stated rather than hidden: the barrel pass still counts prose mentions as calls, so its 43 is a floor, not a measurement — sharpening it would move a calibrated baseline for reasons unrelated to any ticket, and is filed as W12-39.",
  "$visualGateNote": "W13-07, 2026-08-18: the acceptance walker had written 21 frames on every run for waves and NOTHING ever compared them. That is how a UI reaches 'it looks horrible' with 42 style guards green — the guards check declarations, the frames checked nothing, and no gate closed the loop between them. Worse, found while building this: the walker itself had been broken since W12-36 renamed 'New Product', and again by W12-41's form change, because a capture script nothing runs stops working and says nothing. NO NEW DEPENDENCY: Playwright emits 8-bit truecolour non-interlaced PNG (verified from a real capture's IHDR), which node:zlib plus the five PNG filters decodes in ~60 lines; pixelmatch + pngjs would have been a TECH_STACK decision, and this repo ships five runtime dependencies on purpose. THRESHOLD MEASURED, NOT GUESSED: two captures of identical code showed 16 of 21 frames pixel-identical and a worst case of 0.21% (timestamps and generated project ids), so 0.5% is ~2.4x the floor while still failing on anything structural — the smallest real change in this wave moved several percent. Cost: ~17s to capture, under a second to compare. It writes to a temp dir via --out, because capturing into docs/acceptance/runs/ on every ticket would have added a megabyte of unread images per ticket. A DIFF IS NOT AUTOMATICALLY A DEFECT: an intended change is accepted BY NAME with --accept --ticket --reason, which records who changed the screen and why beside the baseline, rather than letting someone silently overwrite it.",
  "$gatesNote": "2026-07-15: board validators added to the per-ticket gate — an agent's plan.json/notes edits must keep the citation graph green (caught W3-10 hallucinating docs/MODULE_DESIGN.md only on manual re-run; now automatic).",
  "$alwaysOkNote": "2026-07-16: +events/migrations/** — numbered migrations are append-only shared infra (like pnpm-lock); every ticket adding a table needs a new NNN_*.sql without a per-ticket scope collision (L-04/W4-06). The migration RUNNER (packages/events/src/db.ts) stays out — only the numbered files.",
  "$alwaysOkAddOnlyNote": "W11-09, 2026-08-06: alwaysOk is pure path matching with no added-vs-modified distinction, so events/migrations/** being alwaysOk also exempted EDITING an already-shipped migration in place — silent because applyMigrations keys off PRAGMA user_version plus the file's numeric prefix, so an in-place edit looks applied in a fresh checkout/CI and is absent on every state.db that already ran the original. alwaysOkAddOnly names the subset of alwaysOk entries where scripts/conductor/gates.mjs additionally requires git status 'A' (a pure addition) before exempting a path — M/D/R on an existing file there stays out-of-scope unless write_scope names it. Deliberately a NEW key, not a change to alwaysOk itself or globToRegex: the rest of alwaysOk (the board, the lockfile, tsconfig, ...) is genuinely modify-in-place infra and must keep exempting M/D. History check (git log --diff-filter=M -- packages/events/migrations/) before filing this note: three prior modifications exist (f3cbc03 W0-02, d522708 W10-68, 8d0f96c the Shipwright->Dokima rename) and none exploited this hole — the first two are same-ticket same-session amendments to a migration that ticket itself had just added (W10-68's write_scope explicitly covered migrations, confirmed in its own ticket notes), and the rename commit is a direct-to-main scripted maintenance pass outside the ticket/gate mechanism entirely, touching only a SQL comment (a package name string), never schema. The hole existed but was never exercised.",
  "$migrationScopeNote": "W11-08, 2026-08-06: ON. W10-68 self-blocked twice (2026-08-04, 2026-08-05) because its write_scope could not reach a migration file its own acceptance required — visible in the ticket text without running anything. This rule warns at filing time instead. Deliberately NOT fixed by teaching MASTER_PROMPT.md about alwaysOk's packages/events/migrations/** entry: alwaysOk is a GATE-SIDE amnesty (scripts/conductor/gates.mjs) the maker session never sees, and MASTER_PROMPT.md correctly tells the maker to stay in write_scope and report rather than widen it — both of W10-68's blocks were the right behaviour under that instruction. Loosening the prompt to mention alwaysOk would stop this specific block and license silent scope drift on every other ticket. dir points at this repo's one migrations directory (packages/events/migrations/, single numbered .sql files, no up/down pairs) — see the precedent tickets W0-05/W3-03/W5-13 for what a correctly-scoped migration ticket looks like.",
  "$testSiblingNote": "W11-08, 2026-08-06: ON, source/test = *.ts(x) / *.test.ts(x). Measured against the live board before enabling (not assumed): of 8 open (non-done) tickets, exactly 1 (W11-04) currently lacks a test sibling in its implementation file's directory — done tickets are silently skipped, so this is a handful of warnings, not noise across 185 closed tickets.",
  "$pageMountNoteW11-08": "W11-08, 2026-08-06: left OFF, and this is a decision, not an oversight. pageMountWarning models Kryptkeeper's routed multi-page UI (a new file under a pages/ glob needs a route + nav-menu entry). apps/web has no router (grep for react-router/createBrowserRouter/<Routes> across apps/web/src returns nothing) and no pages/ directory convention — it is a single-page canvas app (App/AppShell.tsx + MainView.tsx composing panes), with exactly one *Page.tsx file (SettingsPage.tsx) in the whole tree. The rule's premise (new page file -> needs mounting) does not describe how this UI is built, so enabling it would warn on a shape that never occurs here rather than catch a real gap. Revisit if apps/web ever grows a router.",
  "$modelsNote": "W9-12: per-role model routing used to live in a separate scripts/models.json, read unconditionally at module scope in conductor.mjs — a repo importing only conductor.mjs + conductor-lib.mjs + this config crashed with a bare ENOENT before --lint or any argument parsing ran (found live importing into Kryptkeeper 2026-07-28). Folded in here so the $note's 'everything project-specific lives here' claim is true of models too; conductor-lib.mjs's DEFAULT_CONFIG.models supplies a generic fallback ladder for repos that omit this key. cheapMaxPoints 2->1 2026-07-14 (amplifier LESSONS 25: sonnet is the floor for release tickets; haiku trivial-only). Overnight runs launch WITHOUT --escalate: sonnet-only ladder, frontier spend requires a human (escalation-token principle by configuration, D-018).",
  "$holdNote6": "2026-08-13: W12-06 HELD at filing time, not after a run tripped over it. It asks which of 93 experts should own a given ticket — a routing-policy call with three named candidate answers in its acceptance (explicit ticket field / lane inference / model-side selection), each with a different blast radius on the board schema, the decomposer and Law 4. Every one of them passes every gate, which is exactly the $holdNote5 shape: no test can see a routing policy be the wrong routing policy. An unattended session would pick one and make it permanent by implementing it. Un-hold once the direction is decided. NOTE the sibling tickets from the same review are deliberately NOT held: W12-04 (wire the Context Packer) and W12-05 (wire the tool anchor) both have their design decision already made and written into acceptance — W12-04 in particular states that building in apps/server rather than amending the harbormaster->memory matrix edge is settled and must not be re-opened, precisely so it stays claimable.",
  "$holdNote5": "2026-08-13: W12-01 HELD, and W10-58 dropped from the list (done 2026-08-07). The board was drained 201/201 before W12-01 was filed, so this ticket is the ONLY thing a conductor run can claim — an unattended launch would go straight to it. Its acceptance clause 3 says the fix direction is a call to check against ARCHITECTURE's dependency matrix first (`api/` importing from `bootstrap/` may be the wrong way round, in which case the constant moves to a shared module both import). That is the $holdNote4 shape exactly: structurally claimable, actually waiting on a human. An unattended session would pick a direction, and every gate would pass either way — the whole point of the ticket is that no test can see this constant disagree with itself. Un-hold once the direction is decided, or work it interactively.",
  "$holdNote4": "2026-08-05, RESOLVED SAME DAY — W11-02 is UN-HELD; kept here because the failure mode it records will recur. The founder took the documented path: docs/ARCHITECTURE.md §3.6 already said tool allowlisting must reuse packages/mcp 'rather than a parallel mechanism', so the matrix ✅ and the @dokima/mcp declaration were made together as one chore, and W11-02 now consumes an existing dependency instead of amending the architecture mid-ticket. THE RECURRING PART: a ticket can be structurally claimable and still be waiting on a human, and the board has no field for that — holdTickets is the only place it can be said. Original note follows. 2026-08-05: W11-02 HELD, and the reason is a decision that belongs to a human, not a missing dependency. W11-05 made package.json authoritative and added a validate-plan P10 check that FAILS when a package declares a dependency the ARCHITECTURE matrix forbids. The corrected matrix does not permit harbormaster -> mcp, so W11-02's plan to reuse packages/mcp for its tool allowlist now needs an explicit matrix amendment AND the declaration together — a deliberate architectural change. Its depends_on (W11-01, W11-05) are both done, so claimableTickets considers it claimable and sorts it FIRST of the open W11 tickets; a run launched to work W11-06 claimed W11-02 instead and was stopped ~2s in. Un-hold once the matrix call is made. Note that the conductor's claim logic is deliberately looser than validate-plan.mjs's 'Claimable now' report, which additionally requires the wave to be open — the report says only W10-58 while the conductor sees all four open W11 tickets; do not use the report to predict what the conductor will claim.",
  "$holdNote3": "2026-08-04: W10-58 HELD from unattended claim. Its premise was re-measured and its acceptance rewritten the same day — the timeout justification is dead and the remaining requirement is to GENERALISE W10-67 paused-run machinery rather than re-solve it. That is a reading-comprehension dependency on another ticket, on an 8-point architectural change touching packages/events, and it is the kind of nuance an unattended session misses while still producing something that passes gates. Founder chose a held, one-ticket, no-push first run."
}
